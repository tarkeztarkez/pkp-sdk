{
  "name": "@pkp-sdk/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "pkp": "./src/index.ts"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "typecheck": "tsc -p ./tsconfig.json",
    "test": "bun test ./src",
    "start": "bun run ./src/index.ts"
  },
  "dependencies": {
    "@pkp-sdk/core": "workspace:*",
    "@pkp-sdk/api": "workspace:*"
  }
}
